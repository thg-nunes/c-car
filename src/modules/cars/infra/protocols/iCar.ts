type Car = {
  name: string;
  description: string;
  available: boolean;
  daily_rate: number;
  license_plate: string;
  fine_amount: number;
  brand: string;
  category_id: string;
};

export { Car };
